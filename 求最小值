#include<iostream>
using namespace std;
int min(int,int);
double min(double,double);
int main()
{
	cout<<min(2,3)<<endl;
	cout<<min(2.5,3.5)<<endl;
	system("pause");
	return 0;
}
int min(int a,int b)
{
	return(a>b?b:a);
}
double min(double a,double b)
{
	return(a>b?b:a);
}
